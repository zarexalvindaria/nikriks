import { CustomerManagement } from './customer-management';

describe('CustomerManagement', () => {
  it('should create an instance', () => {
    expect(new CustomerManagement()).toBeTruthy();
  });
});
